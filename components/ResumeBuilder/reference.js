export default function Experience(props) {
    return (
        <div>
            <h1 className="">
                {props.name}
            </h1>
            <p>
                {props.contactInfo}
            </p>
        </div>
    );
}