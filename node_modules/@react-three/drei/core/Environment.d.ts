import * as React from 'react';
import { Scene, Loader } from 'three';
import { PresetsType } from '../helpers/environment-assets';
declare type Props = {
    children?: React.ReactNode;
    near?: number;
    far?: number;
    resolution?: number;
    background?: boolean;
    files?: string | string[];
    path?: string;
    preset?: PresetsType;
    scene?: Scene | React.MutableRefObject<THREE.Scene>;
    extensions?: (loader: Loader) => void;
};
export declare function Environment(props: Props): JSX.Element;
export declare function EnvironmentPortal({ children, near, far, resolution, background, scene, files, path, preset, extensions, }: Props): JSX.Element;
export declare function EnvironmentMap({ background, files, path, preset, scene, extensions, }: Props): null;
export {};
